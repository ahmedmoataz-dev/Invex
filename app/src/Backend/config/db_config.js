const config = {
    user: "ahmed",
    password: "1234",
    server: "localhost",
    database: "Invex_Final", // Seelcted db in the SQL Server login
    options: {
        encrypt: false,
        trustServerCertificate: true
    }
};

export default {
    config
}